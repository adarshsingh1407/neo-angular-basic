'use strict';

describe('Directive: appDirectives', function () {

  // load the directive's module
  beforeEach(module('neoAngularBasicApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

});
